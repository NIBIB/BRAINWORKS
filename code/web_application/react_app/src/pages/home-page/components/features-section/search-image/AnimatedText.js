import ReactTypingEffect from "react-typing-effect";

const TEXTS = ["Brains", "Heart", "Lung", "COVID19"];

const AnimatedText = () => {
  return <ReactTypingEffect text={TEXTS} />;
};

export default AnimatedText;
